terraform {
  backend "s3" {
    bucket       = "your-terraform-state-bucket"
    key          = "s3-cloudfront/terraform.tfstate"
    region       = "eu-west-1"
    encrypt      = true
    use_lockfile = true
  }
}
